// templui component skeleton - version: v1.12.1 installed by templui v1.12.1
// 📚 Documentation: https://templui.io/docs/components/skeleton
package skeleton

import "github.com/FACorreiaa/Aviation-tracker/app/utils"

type Props struct {
	ID         string
	Class      string
	Attributes templ.Attributes
}

templ Skeleton(props ...Props) {
	{{ var p Props }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	<div
		if p.ID != "" {
			id={ p.ID }
		}
		class={
			utils.TwMerge(
				"animate-pulse rounded bg-muted",
				p.Class,
			),
		}
		{ p.Attributes... }
	></div>
}
